<h1>Jogos</h1>

<table>
  <thead>
    <tr>
      <th>Chances</th>
      <th>Palavra</th>
      <th></th>
      <th></th>
      <th></th>
    </tr>
  </thead>

  <tbody>
    <% @games.each do |game| %>
      <tr>
        <td><%= game.chances %></td>
        <td><%= game.word %></td>
        <td><%= link_to 'Ver', game %></td>
        <td><%= link_to 'Editar', edit_game_path(game) %></td>
        <td><%= link_to 'Destruir', game, method: :delete, data: { confirm: 'Você tem certeza?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'Novo jogo', new_game_path %>
